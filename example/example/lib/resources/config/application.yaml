server:
  proxy:
    domain: api.bixterprise.com
    protocole: https
    url: '@{server.proxy.protocole}://@{server.proxy.domain}'


pageable:
  default:
    pageSize: 30
    index: 1


security:

  authorization:
    username: ################# NOTHING :( #####################
    password: ################# NOTHING :( #####################
  type: jwt
